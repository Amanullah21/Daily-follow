//https://practice.geeksforgeeks.org/problems/find-the-string-in-grid/0

const findStringGrid = (grid, str) => {
//   console.log(grid, str);
    let n = grid.length
    for(let i=0;i<n;i++){
        for(let j=0;j<n;j++){

        }
    }
};
const grid = [
  ["a", "b", "c"],
  ["d", "r", "f"],
  ["g", "h", "i"],
];
findStringGrid(grid, "abc");

// not complete